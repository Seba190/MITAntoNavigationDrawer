package com.seba.mitantonavigationdrawer.ui.Reportes.misDatos.almacenes

import android.view.LayoutInflater
import android.view.ViewGroup
import androidx.recyclerview.widget.RecyclerView
import com.seba.mitantonavigationdrawer.R


//class CaracteristicasAdapter (var caracteristicasList: List<CaracteristicasDataResponse> = emptyList())
 //   : RecyclerView.Adapter<CaracteristicasViewHolder>() {
   /* fun updateList2(caracteristicasList: List<CaracteristicasDataResponse>){
        this.caracteristicasList = caracteristicasList
        notifyDataSetChanged()
    }
    override fun onCreateViewHolder(parent: ViewGroup, viewType: Int):CaracteristicasViewHolder {
        val layoutInflater = LayoutInflater.from(parent.context)
        return CaracteristicasViewHolder(layoutInflater.inflate(R.layout.item_almacen,parent,false))
    }

    override fun onBindViewHolder(viewholder: CaracteristicasViewHolder, position: Int) {
        val item = caracteristicasList[position]
        viewholder.bind2(item)
    }

    override fun getItemCount() = caracteristicasList.size*/

//}